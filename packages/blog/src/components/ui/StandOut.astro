---
import Card, { type Props as CardProps } from "./Card.astro";

type Props = CardProps;

const { class: className, ...rest } = Astro.props;
---
<Card class:list={["mx-3", className]} {...rest}>
	<slot/>
</Card>
